# Dsphr Home 界面修复清单（给你直接评论/改的版本）

> 你只需要做两件事：  
> 1）看懂每条“要做什么”；  
> 2）在 `TODO/备注/验收` 里写结论或确认点。  
> 这份文档不会自动改代码，它只是给团队对齐任务用。

---

## 说明：每条都长这样
- **要做什么**：一句话说清楚任务
- **在哪改**：大概改哪个文件/模块
- **TODO（你/团队要决定什么）**
- **状态**：`todo / doing / done`
- **验收（怎么证明改好了）**

---

## P0（必须先做）：让项目能正常跑

### 1. 修复编译/类型错误
- **要做什么**：修掉 `npm run lint` 里那些 TypeScript 报错，让项目能编译通过
- **在哪改**：
  - `src/pages/UserHome/WorkbenchPage.tsx`
  - `src/services/leads/nonDemoProjectFormBaseline.ts`
- **TODO**
  - 把所有 `npm run lint` 报错逐条修完
  - （如果你想先修某几个关键错误，把“优先级顺序”写在备注里）
- **状态**：`todo`
- **负责人**：
- **备注/评论**：
  - 你可以把当前最烦人的报错复制到这里
- **验收**
  - 执行 `npm run lint`：不再报错
  - 执行 `npm run dev`：页面能打开（至少关键页面不要直接报错白屏）

---

## P1（强烈建议）：让“普通人”更好用/看得清

### 2. 让浅色背景上的字更清楚（对比度问题）
- **要做什么**：把浅色背景上的“次要文字”调深一点，避免费眼
- **在哪改**：
  - `src/index.css`（`--color-sub`）
  - `src/components/DeepEvalForm.tsx`（使用了 `text-sub` 的副文案）
- **TODO**
  - 让副文案在当前米白底上“看得清”
  - 你可以在这里决定：副文案是“更深一点”，还是“换成别的语义颜色”
- **状态**：`todo`
- **负责人**：
- **备注/评论**：
  - 你希望达到的效果：更深一点 / 保持温暖但更清楚？
- **验收**
  - 目测：不用眯眼就能清晰读完副文案
  - 不要出现“灰到看不清/橙字看不清”的情况

### 3. 选中态（橙色）也要清晰可读
- **要做什么**：改选中态颜色组合，保证“看得到 + 读得清”
- **在哪改**：`src/components/ui.tsx`（例如 `RadioCard` 的选中态）
- **TODO**
  - 你希望选中态更像：更深的橙字 / 橙色边框更明显 / 或改背景？
- **状态**：`todo`
- **负责人**：
- **备注/评论**：
  - 你偏好的视觉效果写一下
- **验收**
  - 鼠标/键盘切换选项时，选中状态明显且不糊

### 4. 键盘 Tab 跳转时：要能看出“现在在哪”
- **要做什么**：给自定义按钮/卡片补上清晰的“焦点框”（键盘操作必需）
- **在哪改**：`src/components/ui.tsx`（各种自定义 `button` 卡片）
- **TODO**
  - 给所有可点击卡片加：`Tab` 走过去时能看到明显边框/高亮
- **状态**：`todo`
- **负责人**：
- **备注/评论**：
  - 你想要焦点框是什么风格（品牌橙 ring / 其他）？
- **验收**
  - 只用键盘：`Tab/Shift+Tab` 能准确知道当前停在哪个按钮/卡片

### 5. 弹窗（比如反馈/签字/目录弹层）要更“懂无障碍”
- **要做什么**：让弹窗对屏幕阅读器和键盘都“可理解、可操作”
- **在哪改（抽查到的）**：
  - `src/components/FeedbackModal.tsx`
  - `src/components/SignatureModal.tsx`
  - `src/components/NavigationMenu.tsx`
  - `src/pages/Quotation/components/FeedbackModal.tsx`（同类重复）
- **TODO**
  - 打开弹窗：焦点进入弹窗（不要跑到弹窗外）
  - 关闭弹窗：焦点回到正确位置
  - 弹窗要有“这是弹窗”的语义（dialog）
  - （如你们期望）`Escape` 可以关闭
- **状态**：`todo`
- **负责人**：
- **备注/评论**：
  - 关闭后回焦点：回到“触发按钮”还是回到弹窗之前的最后位置？
- **验收**
  - 键盘打开弹窗：焦点在弹窗里，能正常操作
  - 关闭弹窗：焦点回到原来的地方
  - 屏幕阅读器能读出弹窗标题/内容（不乱跳）

### 6. 系统“减少动态效果”时：动画别太多/别转来转去
- **要做什么**：照顾容易晕动的人
- **在哪改**：全局（尤其是使用 `motion` 的地方）
- **TODO**
  - 当系统选择“减少动态效果”时：明显减少/停用旋转、位移等动效
- **状态**：`todo`
- **负责人**：
- **备注/评论**：
  - 你希望：完全停掉动效，还是只保留轻量淡入？
- **验收**
  - 开启系统 reduced motion 后：页面不会让人不舒服，但交互仍清楚可用

---

## P2（后续优化）：让体验更顺更稳

### 7. 手机上按钮太小：适当扩大可点区域
- **要做什么**：减少误触
- **在哪改**：`src/components/ui.tsx`（例如 `Counter` 的 `- / +`）
- **TODO**
  - 把关键按钮可点区域做大（视觉可以不变，但点击面积要更友好）
- **状态**：`todo`
- **负责人**：
- **备注/评论**：
  - 哪些按钮最常用、最需要优先变大？
- **验收**
  - 手机上点起来更稳，不需要精准对准

### 8. 图片性能：非首屏加 `lazy loading`
- **要做什么**：减少首屏压力，让滚动更顺
- **在哪改**：`src/pages/StyleEval/components/QuestionCard.tsx`（抽查到 `<img>`）
- **TODO**
  - 非首屏图片加 `loading="lazy"`
- **状态**：`todo`
- **负责人**：
- **备注/评论**：
  - 如果你有性能指标（比如首屏速度目标），可以写这里
- **验收**
  - 滚动更顺，首屏加载更快/更稳定

---

## 你先回答我一个问题（方便我后续给你更准确的“改哪些”）
- 你想先从哪个开始做？
  - `A` 先过 `npm run lint`（P0）
  - `B` 先改对比度/让字更清楚（P1）
  - `C` 先改键盘焦点/弹窗无障碍（P1）
  - `D` 你有别的优先级

